spring:
  application.name: CarPark
  datasource:
    url: jdbc:postgresql://localhost:5552/carpark
    username: postgres
    password: Ffijvd5555
  jpa:
    generate-ddl: true
    show-sql: true
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 64MB
      max-request-size: 64MB
  http:
    multipart:
      enabled: true